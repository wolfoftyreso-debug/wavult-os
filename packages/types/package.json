{
  "name": "@hypbit/types",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "build": "tsc --declaration --emitDeclarationOnly --outDir dist"
  }
}
