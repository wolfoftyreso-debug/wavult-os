# BOS n8n Workflows

Import these into n8n via Settings → Import workflow.

## Workflows
- `bos-deadline-escalation.json` — Triggered when a task deadline passes
- `bos-state-reconcile.json` — Triggered for state reconciliation

## Environment Variables (set in n8n)
- `SUPABASE_SERVICE_KEY` — Wavult OS service role key
